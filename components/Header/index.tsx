import { View } from 'react-native'
import { Text } from '../../global/globalStyle'

// Functional Component
export default function Header() {
  // Rendering
  return (
    <View>
      <Text size="base">Header</Text>
    </View>
  )
}
